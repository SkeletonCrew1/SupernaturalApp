# What is?
This is Terraform code implementation for application S3 bucket where it stores images.

# How to run?
1. Log into AWS CLI using `aws configure` and specifying correct credentials.
2. Run `terraform apply` and approve by typing `yes` when prompted.
3. Wait for Terraform to finish creating resources.
