apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceDefaults
metadata:
  name: auth
  namespace: app
spec:
  protocol: http
---
apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: auth
  namespace: app
spec:
  destination:
    name: auth
  sources:
    - name: api-gateway
      action: allow
    - name: frontend
      action: allow
    - name: "*"
      action: deny
